# V4 Dataset Report

> Generated by `python scripts/prepare/build.py`. Every number below is a count of rows in `datasets/manifests/all.csv`. **No model was trained and no dataset was downloaded to produce it.**

## 1. Totals

| | |
|---|---|
| Manifest rows (nothing is ever deleted) | **519** |
| Accepted - eligible for train/validation | **511** |
| Sealed - evaluation only | **0** |
| Rejected - with a reason | **6** |
| Independent groups (the honest sample size) | **505** |

## 2. Labels

| Label | Count | Share |
|---|---|---|
| `REAL` | 500 | 97.8% |
| `FAKE` | 11 | 2.2% |

## 3. Datasets

| Dataset | Count | Share |
|---|---|---|
| `lfw` | 498 | 97.5% |
| `local_clips` | 8 | 1.6% |
| `generated` | 5 | 1.0% |

**Dataset share of the accepted pool** — no single source may quietly dominate:

| Share | Count | Share |
|---|---|---|
| `lfw` | 97.5 | 97.5% |
| `local_clips` | 1.6 | 1.6% |
| `generated` | 1.0 | 1.0% |

## 4. Manipulation

| Family | Count | Share |
|---|---|---|
| `REAL` | 500 | 97.8% |
| `GAN` | 11 | 2.2% |

| Method | Count | Share |
|---|---|---|
| `(none)` | 500 | 97.8% |
| `stylegan2` | 6 | 1.2% |
| `tpdn` | 5 | 1.0% |

## 5. Compression

| Compression | Count | Share |
|---|---|---|
| `jpeg` | 503 | 98.4% |
| `unknown` | 8 | 1.6% |

## 6. Face detection

| | |
|---|---|
| Faces found | 517 |
| No face - kept for inspection, never trained on | 0 |
| More than one face in frame | 97 |

## 7. Rejections

Every rejected sample keeps its manifest row and its reason, so the corpus stays reconstructible.

| Reason | Count | Share |
|---|---|---|
| `NEAR_DUPLICATE` | 4 | 66.7% |
| `UNSAFE_GROUP` | 2 | 33.3% |

## 8. Duplicates

| | |
|---|---|
| Exact duplicate sets (SHA-256) | 1 |
| Near-duplicate pairs (dHash <= 4, within group) | 4 |
| **Duplicates spanning two groups** | **1** |

A duplicate spanning two groups means the group key is wrong and no split can be trusted; the build stops if any is found.

## 9. Split

| | |
|---|---|
| Algorithm | sort groups, shuffle with seed, take groups until the validation row target is met; rows follow their group |
| Seed | `42` |
| Train rows | 408 |
| Validation rows | 103 |
| Train groups | 402 |
| Validation groups | 103 |
| Validation share requested | 20% |
| Validation share achieved | 20.2% |

The exact percentage is not forced. A split that hits 20.0% by cutting a group in half is worse than one that lands at 22% and keeps identities whole.

## 10. Seal

| | |
|---|---|
| Sealed datasets | celebdf, deeperforensics |
| Withheld FF++ method | `neuraltextures` |
| Sealed groups recorded | 1 |
| Seal file | `docs/v4_sealed_groups.txt` |

## 11. Reproducibility

```bash
python scripts/prepare/build.py --all --frames 8
```

Deterministic by construction: frame indices are uniform rather than random, `sample_id` is a hash of dataset + path + frame index, and the split shuffles a *sorted* group list with seed `42` so filesystem order cannot influence it.

## 12. What is still unknown

- Any dataset marked `absent` above contributed nothing to these counts. See `V4_DATASET_PROVENANCE.md` for access status.
- Samples rejected as `UNSAFE_GROUP` had no group key in their metadata. They are recorded, not guessed at.
- `manipulation_family` is `UNKNOWN_FAKE` wherever the dataset does not state the method.
