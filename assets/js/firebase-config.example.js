/* ============================================================
   DeepShield AI — Firebase configuration

   HOW TO FILL THIS (one-time, ~10 min):
   1. https://console.firebase.google.com → Add project
   2. Build → Authentication → Get started
      → Sign-in method → enable "Email/Password" and "Google"
   3. Project settings (gear icon) → Your apps → Web app (</>)
      → Register app → copy the firebaseConfig values below
   4. Paste them here, replacing the PASTE_ placeholders.

   While these placeholders remain, the app automatically runs in
   DEMO login mode (any credentials work, everything stays local).
   ============================================================ */

window.DS_FIREBASE_CONFIG = {
  apiKey: "PASTE_YOUR_API_KEY",
  authDomain: "PASTE_YOUR_PROJECT.firebaseapp.com",
  projectId: "PASTE_YOUR_PROJECT_ID",
  appId: "PASTE_YOUR_APP_ID",
};
