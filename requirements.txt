# DeepShield AI — Python dependencies
#
#   Windows:      venv\Scripts\pip install -r requirements.txt
#   macOS/Linux:  venv/bin/pip install -r requirements.txt
#
# On Linux, plain `torch` pulls the ~2 GB CUDA build. This project is
# CPU-only, so install those two from the CPU index instead:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# (Windows and macOS wheels on PyPI are already CPU builds.)

# ---- Web server ----
flask>=3.1

# ---- AI engine ----
torch>=2.0
torchvision>=0.15
opencv-python>=4.8
pillow>=10.0
numpy>=1.24

# ---- Ensemble verifiers (HuggingFace; optional — the app runs without
#      them, using our own model alone) ----
transformers>=4.40

# ---- Optional: server-side PDF reports (not used yet; the report page
#      prints to PDF from the browser) ----
# reportlab
